spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/minicampus
    driver-class-name: org.mariadb.jdbc.Driver
    username: minicampus_user
    password: zerobase
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: kis42303569@gmail.com
    password: almefiaorguzvgqa
    properties:
      mail:
        smtp:
          starttls:
            enable: true

logging:
  level:
    root: info
